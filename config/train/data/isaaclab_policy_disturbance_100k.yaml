dataset:
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 1
  name: isaaclab_policy_disturbance_100k.h5
  keys_to_load:
    - pixels
    - action
    - prediction_valid
  keys_to_cache:
    - action
    - prediction_valid
