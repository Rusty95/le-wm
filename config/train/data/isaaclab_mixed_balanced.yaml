dataset:
  num_steps: ${eval:'${num_preds} + ${history_size}'}
  frameskip: 1
  names:
    - isaaclab_random_100k.h5
    - isaaclab_policy_camera_50k.h5
  balance: interleave
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
